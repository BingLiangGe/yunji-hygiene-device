server:
  port: 62521
netty:
  port: 62522
  threads:
    boss: 1
    worker: 4
    business: 20 #业务线程数量
  time:
    # 所有操作时间限制(单位秒) 0禁用
    all: 0
    # 读取操作时间限制(单位秒) 0禁用
    reader: 60
    # 写入操作时间限制(单位秒) 0禁用
    writer: 0
spring:
  jpa:
    database: mysql
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect